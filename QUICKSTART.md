# 快速开始指南（简化版）

## 第一步：安装依赖

```bash
npm install
```

## 第二步：构建项目

```bash
npm run build
```

构建完成后，所有文件会输出到 `dist` 目录。

## 第三步：加载插件

1. 打开 Chrome 浏览器
2. 在地址栏输入 `chrome://extensions/` 并回车
3. 开启右上角的"开发者模式"开关
4. 点击"加载已解压的扩展程序"按钮
5. 选择项目的 `dist` 目录

## 第四步：配置 API 地址

1. 右键点击浏览器工具栏中的插件图标
2. 选择"选项"
3. 输入后端 API 的基础 URL（例如：`http://localhost:3000/api`）
4. 点击"保存设置"

## 第五步：使用插件

1. 访问任意网页
2. 点击浏览器工具栏中的插件图标
3. 在弹出窗口中输入任务描述（例如："提取页面中的所有产品信息"）
4. 点击"开始分析"按钮
5. 插件会自动：
   - 爬取当前页面内容
   - 发送到后端 API 进行分析
   - 执行 AI 返回的操作（如点击、滚动等）
   - 显示分析结果

## 开发模式

在开发过程中，可以使用监听模式自动重新构建：

```bash
npm run dev
```

修改代码后，在 Chrome 扩展程序管理页面点击插件的"重新加载"按钮即可看到更改。

## 学习建议

1. **先看代码注释**: 所有文件都有详细的中文注释
2. **理解消息流**: 
   - popup → background → content
   - content → background → popup
3. **从简单开始**: 先理解每个文件的基本功能，再逐步深入
4. **动手实践**: 尝试修改代码，看看会发生什么

## 常见问题

### Q: 为什么看不到插件图标？
A: 需要先添加图标文件到 `public/icons/` 目录（icon16.png, icon48.png, icon128.png）

### Q: 构建失败怎么办？
A: 确保已运行 `npm install` 安装所有依赖

### Q: 如何调试？
A: 
- Background script: 在 `chrome://extensions/` 中点击"service worker"
- Content script: 在网页中按 F12 打开开发者工具
- Popup: 右键点击插件图标 → "检查弹出内容"
